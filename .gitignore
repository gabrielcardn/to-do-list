# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json
pids
*.pid
*.seed
*.pid.lock

# Cache de dependências e arquivos de lock (se não compartilhados ou para evitar conflitos de merge)
# package-lock.json # Descomente se você não quiser versionar o lock file (geralmente é versionado)
# yarn.lock         # Descomente se você não quiser versionar o lock file (geralmente é versionado)
# pnpm-lock.yaml    # Descomente se você não quiser versionar o lock file (geralmente é versionado)

# Pasta de dependências
node_modules/
jspm_packages/
web_modules/

# Arquivos de ambiente (CONTÊM SEGREDOS!)
.env
.env.local
.env.*.local
.env.development
.env.test
.env.production
# Exceto .env.example ou .env.template (se você tiver)
!.env.example
!.env.template

# Pastas de build/distribuição
dist/
build/
out/
coverage/
.angular/
.next/
public/build/ # Para Create React App
*.vite.*

# TypeScript cache
*.tsbuildinfo
.tmp/

# Arquivos de sistema operacional
.DS_Store
Thumbs.db

# Arquivos de IDE
.idea/
.vscode/ # Descomente se você não quiser compartilhar configurações do VSCode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Arquivos de teste específicos
junit.xml